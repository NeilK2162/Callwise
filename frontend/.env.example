# Base URL of the Callwise control-plane API. If unreachable, the dashboard falls back
# to the seeded example calls in mocks/seed.ts so the feed always looks alive.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
